<!DOCTYPE HTML>
<html>
  <body>
    <div><i>&nbsp;Here are your bookings, <%= current_user.username %></i></div>
        <% @bookings.each do |booking| %>
        <% if booking.user_id == current_user.id %></br>
          <div class="box">
          <% bookers_spaces = Space.all(:id => booking.space_id) %>
          <% bookers_spaces.each do |spc| %>
            <span class="space-listing">Name:</span> <%= spc.name %></br>
            <span class="space-listing">Description:</span> <%= spc.description %></br>
            <span class="space-listing">Price per night:</span> £<%= spc.price %></br>
            <span class="space-listing">Requested from:</span> <%= spc.from_date.strftime('%d %B %Y') %></br>
            <span class="space-listing">Requested to:</span> <%= spc.to_date.strftime('%d %B %Y') %></br>
            <% if booking.confirmed %></br><div class="conf-true">This booking has been confirmed by the owner.</div>
            <% else %></br><div class="conf-false">This booking request has not been accepted yet.</div>
              <% end %></div>
            <% end %>
          <% end %>
      <% end %>
  </body>
</html>
